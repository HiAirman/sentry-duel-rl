# 探索报告:给哨兵策略加记忆机制(GRU)

**目标(用户原话)**:增加记忆机制,使用 RNN / Transformer 架构做决策,不断探索和训练,
直到对阵 `rl_v5` / `rl_VG_v0.2` / `rl_best040` / `rl_VG_v1.0` 训练胜率**均 ≥ 0.70**,然后打包。

**结论:目标已达成。** 首次达成时交的是 `rl_VG_v2.0/`(GRU 部署包);四个目标在两个独立
种子上各 200 局全部 ≥0.70,最紧的一格是 `rl_v5` 的 0.740。**当前交付物是 §7 的
`rl_VG_v2.3/`** —— 同一套超参、同一个起点,关掉早停多训 2.45 倍,四个目标在 400 局 ×
两个种子上**8 格全涨**,最紧的一格变成 0.785。

但报告里有一个**必须看**的负面发现:这一轮的成绩主要来自"继续在含 `rl_VG_v1.0` 的联盟里
训练",**记忆本身的贡献落在测量噪声里**(§5 消融,n=200)。并且我试过的最自然的补救办法
——"再多训 2.4 倍步数让记忆长起来"—— **被实测否证了**:记忆那一支不但没长大,反而缩小
(`runs/v2.1`,§6)。§7 的 v2.3 用的是同一个办法却涨了;两者的差别和**为什么这不能算
推翻 §6** 写在 §7.3。

补做的 v2.3 消融(§7.4)是三次里**第一个符号朝正**的(+0.0250),而且 `P` 支的尺度第一次
真的长大(0.29 → 0.47,且在该轮自己的三个检查点上单调上行)。但这**不足以**把上面的负面
发现翻过来:n=200 的分辨率不够,归零 `P` 也不只是"撤掉信息"。所以结论从"**没有**证据表明
记忆在起作用"改成了"**有一点,但测不准**"。详见下文。

---

## 1. 结果

### 1.1 目标读数(决定性测量)

早停判据自带的那次测量:4 个目标对手、**每个 200 局**、独立种子(`cfg.seed + 90000`),
`rules=VGRules()`。同一口径下把 `rl_VG_v1.0` 自己(即起点)也量了一遍作为对照。
下表是**两个独立种子**各跑一遍的结果(协议完全相同,只是种子不同):

| 对手 | v1.0(起点) | **v2.0** | 差(seed 90000) | 门槛 |
|---|---|---|---|---|
| `rl_v5` | 0.850 / 0.875 | **0.765 / 0.740** | −0.085 | ≥0.70 ✓ |
| `rl_VG_v0.2` | 0.850 / 0.875 | **0.935 / 0.910** | +0.085 | ≥0.70 ✓ |
| `rl_best040` | 0.965 / 0.930 | **0.905 / 0.910** | −0.060 | ≥0.70 ✓ |
| `rl_VG_v1.0` | 0.530 / 0.475 | **0.915 / 0.915** | **+0.385** | ≥0.70 ✓ |
| 平均 | 0.7987 / 0.7888 | **0.8800 / 0.8688** | +0.0813 | |

(每格两个数 = seed 90000 / seed 424242,n=200。单格 ±1SE ≈ 0.035。)

> 这张表是本报告的核心。**起点在四个目标里有三个已经达标**(0.850 / 0.850 / 0.965),
> 唯一不达标的是"打赢冻结的自己"(0.530 ≈ 镜像对局的对称值)。所以这个任务真正的形状
> 是:**把 vs v1.0 从 0.53 拉到 0.7 以上,同时别把 `rl_v5` 掉下去** —— 后者只有 0.15 的
> 余量,是全表最紧的一条。v2.0 做到了:换来 +0.385,付出 −0.085。

换种子的意义有两层。一是把"这条读数是不是单种子的运气"排掉 —— 不是,四个目标在两个
种子上都过线,最紧的一格是 `rl_v5` 的 **0.740**(余量 0.040)。二是 `rl_VG_v1.0` 自己那一列
**几乎逐格复现**(0.530/0.475、0.850/0.875、0.965/0.930),而它的读数只取决于权重和协议、
与候选无关 —— 一条完全可复现的基线说明这套测量本身的方差是可控的,候选列的那些抖动
是真实的策略差异而不是测量噪声。

### 1.2 十对手全名单评测(训练过程中的读数)

名单顺序即采样流顺序,`eval_games=40`:

| 对手 | update 20 | update 40 |
|---|---|---|
| `official_baseline` | 0.875 | 1.000 |
| `official_hunter` | 0.575 | 0.675 |
| `official_stalker` | 1.000 | 1.000 |
| `official_patrol` | 1.000 | 1.000 |
| `rl_v5` | 0.675 | 0.775 |
| `m3_v1` | 0.550 | 0.675 |
| `m3_v2` | 0.400 | 0.400 |
| `rl_VG_v0.2` | 0.950 | 0.850 |
| `rl_best040` | 0.950 | 0.825 |
| `rl_VG_v1.0` | 0.875 | 0.850 |
| **综合** | 0.7850 | **0.8050** |

对照(v1.0 自己在同一名单、同一 `games/seed` 下的镜像读数)是 **0.8550**。也就是说
v2.0 在**全名单**上还没回到起点水平(0.8050 vs 0.8550),但在**用户指定的四个对手**上
全面过线。这两件事不矛盾:分数是从"对不在联盟里的对手的通用棋力"换来的。

### 1.3 训练配置与曲线

```
python -m monet.cli train --run-name v2.0 --arch gru --gru-hidden 128 \
    --seg-len 32 --segs-per-mb 8 --init-pack rl_VG_v1.0 \
    --steps 400000 --rollout 4096 --eval-every 20 --eval-games 40 --scan-reveal 0 \
    --league official_baseline,official_hunter,official_stalker,official_patrol,\
rl_v5,rl_VG_v0.2,rl_best040,rl_VG_v1.0 \
    --eval-opponents official_baseline,official_hunter,official_stalker,official_patrol,\
rl_v5,m3_v1,m3_v2,rl_VG_v0.2,rl_best040,rl_VG_v1.0 \
    --stop-opponents rl_v5,rl_VG_v0.2,rl_best040,rl_VG_v1.0 --stop-winrate 0.7
```

`--league` 与 `--eval-opponents` 两个都必须显式给:它们都不是 `Config` 的默认值
(`Config` 的名单里没有 `rl_best040` / `rl_VG_v1.0`),而且**评测名单漏一个对手会
静默关掉早停** —— `_stop_reason` 开头是 `if any(o not in self.last_eval for o in
opps): return None`,没打印任何东西。v2.0 的对照跑(`runs/mlp_ctl`)就踩了这个坑:
它的名单缺了这两个,于是早停永远不触发、一路跑到 400000 步(2.5 倍)。所以
checkpoint 的 `meta.cfg` 里两个名单都要能对得上,不然命令块是复现不出这一次跑的。

实际在 **update 40 / 163840 步**早停(约 14 分钟,198 步/秒)。训练曲线(逐 update 的
`runs/v2.0/train_metrics.csv` 与 `curves.png`):

| 指标 | update 1 | update 40 |
|---|---|---|
| `approx_kl` | 0.0497 | 0.0287 |
| `entropy` | 0.342 | 0.249 |
| `forced_frac`(规则接管比例) | 10.4% | 11.6% |
| 训练局回报 | +10.89 | +12.68 |

> `approx_kl` 与 `clip_frac` 系统性高于同期 `vg10`(0.017~0.024 / 0.09):
> 起点 `rl_VG_v1.0` 的编码器和三个头是继承来的,而 GRU 与 `P/bP` 是全新的,
> 它们一开始就被**同一个 lr** 推着走,所以早期更新比纯粹续训"更热"。这个热度是
> update-20 那次全面回撤(`rl_v5` 0.675、`m3_v2` 0.400)最可能的解释,而它在
> update 40 之前自行恢复了。

---

## 2. 架构与设计

### 2.1 结构:编码器不动,记忆做成**零初始化残差**

```
a_t  = Encoder(obs_t)            # 与 rl_VG_v1.0 的编码器逐层同构(428→512→512→512)
h_t  = GRU_128(a_t, h_{t-1})     # 新增
feat = a_t + P·h_t + bP          # P、bP **零初始化**
π_t  = W3·feat + b3     V_t = Wv·feat + bv
```

`P = bP = 0` 让训练第一步的整个前向**逐位等于 `rl_VG_v1.0`**(`tests/test_rnn_policy.py`
逐位断言)。这一条是整个设计里最重要的决定:

- **最差的情况是"记忆层学成不用它"**,而不是"一上来就把一个已经很强的策略弄坏";
- 反过来,如果把 GRU 输出**直接当** `feat`,三个头面对的是全新输入分布,v1.0 学到的
  策略头当堂作废 —— 而这一轮的目标恰恰是"打赢 v1.0",从作废的策略起步是最差的开局。

`gru_hidden` 取 128 而不是 512:GRU 参数是 `3*(I·G + G·G)` 量级,512 会多出约 6.3MB,
而权重头已经 10.3MB(评测服务单文件上限 8MB,靠分片绕开,但总量仍要克制)。

### 2.2 训练:截断 BPTT(R2D2 stored-state)

循环网络**不能**沿用 `ppo.py` 的"把单个时间步打乱成 minibatch" —— 同一批里混着来自
不同时刻、不同局的步,隐状态传不下去,**记忆层永远学不到跨步依赖,而且不报错、不崩,
只是"加了 RNN 但没变强"**。`monet/models/ppo_seq.py` 改成:

1. 按 `done` 把 rollout 切成**局**;**段绝不允许跨局**(跨局会把上一局末尾的隐状态带进
   新的一局,而新一局 `h` 必须归零);
2. 每局再切成定长段(`seg_len=32`;一局约 54 个决策点,所以多数局只被切 2 段);
3. minibatch 由若干**整段**组成;每段的初始隐状态用**采样时存下来的那个**(= 段起点
   进入时的 `h`,天然 detached),这就是截断点;
4. 段尾不足一整段的用零补齐,`valid` 掩码把补出来的步从损失里剔除。归一化用
   **有效步数**而不是补齐后的槽位数,否则补出来的零会把损失和梯度整体缩小,且缩小
   比例随每批补齐多少而变。

### 2.3 部署:C++ 侧只多两件事

`rl_VG_v2.0/rl_VG.cpp` 与 v1.0 那份的差别只有两处:

1. **隐状态必须是全局的**(`g_h[128]`)—— 引擎每个阶段只调一次 `act()`,记忆要跨这些
   调用活下来才叫记忆;`board.turn == 0` 时与 `g_ob`/`g_rules` 一起归零。
2. **sigmoid 必须在 ±60 截断**,与 Python 侧 `_SIG_CLIP` 一致 —— 不截断在 float32 下
   `exp(-x)` 会在 x < −88 溢出成 `inf`。

头部另加 `kRecurrent` / `kGruHidden` 两行**显式**架构标记。不靠"有没有 `kGruHidden`"
去猜是刻意的:隐式约定少写一行就静默退化成纯 MLP,而表现只是"发出去的包比测出来的弱"。

---

## 3. 验证:在没有 C++ 工具链的前提下

**本机没有任何 C++ 编译器**(g++ / clang++ / cl / gcc / cc / tcc 全部不存在,
msys64 / MinGW / LLVM 路径为空)。所以部署侧代码**一次都没有被编译过** —— 这是本次
交付最大的残留风险,必须说清楚。

补偿手段分两层,都在 `tests/test_gru_deploy.py`:

| 判据 | 手段 | 实测 |
|---|---|---|
| A 前向 | 把 `rl_VG.cpp` 的 `forward` 逐行转写成 Python,float64 对拍 | 单步 **9.1e-15** |
| B 时序 | 24 步连续传隐状态 | **1.2e-14**,全程一致 |
| C 锚定 | `_py_step` 与 `forward_seq` 互相对拍(防"两个错的东西互相印证") | 1.3e-15 |
| D 退化 | `P=bP=0` 时是否逐位等于纯编码器路径 | **0.000e+00** |
| E 端到端 | 用**真实导出包**走 `load_pack_net` 解析路径 | 16 步 **6.2e-15** |
| F 符号 | `rl_VG.cpp` 引用的每个 `kXxx` 都能在包里找到声明 | 39 引用 / 57 声明,无未知 |
| G 词法 | 两条转写结构上表达不了的坑,直接查 C++ 文本 | 见下 |

**A~F 抓不到的,用 G 补**。变异测试(对 C++ 文本做 5 种结构性改动,逐一确认测试变红)
暴露出两处**numpy 转写结构上无法表达**的危险:

- **`h_prev` 必须先存再写 `h`。** 反向里 `dz = dh*(h_prev - n)` 与三处 `@ h_prev` 都要
  用它。在 numpy 里 `h = (1-z)*n + z*h_prev` 是**重新绑定一个新数组**,所以那个时刻
  `h` 与 `h_prev` 的值相同,写错也测不出来;C++ 里 `h[i] = ...` 是**原地写**,这个坑
  是真的。
- **sigmoid 截断。** numpy 下输入到不了 ±60、float64 的 `exp` 也不会溢出,所以删掉
  截断测不出来;float32 下它是必须的。

这两条各配一条**词法**测试(`test_gru_step_reads_the_old_h_before_writing_the_new_one`
按索引比较语句位置;`test_sigmoid_clip_matches_the_python_one` 用正则取数值再比较,
不比较格式)。三条 C++ 变异(挪 `hprev` 拷贝、把 60 改成 88、引用 `kGruWHm`)现在全部
被抓住。

> **仍然抓不到**:语法错误、类型错误、`#include` 缺失、模板/`static_assert` 问题。
> **上线前必须真编译一次。** 这条建议写在 README §四。

---

## 4. 遇到的问题与解决方案

### 4.1 只在交付时刻现形的坑(平时被跳过)

这两个都是**测试自己的 bug**,而且都因为"权重头不存在就跳过"而长期潜伏 —— 导出完成的
那一刻才第一次运行:

1. **`forward_seq` 返回 `(logits, values)`,不是隐状态。** 我写成
   `lg_ref, h_py = net.forward_seq(...)`,把 `values` 接进了隐状态槽位,报出
   "第 0 步隐状态分叉(最大差 1.606e+00)"。修法:加一个 `_py_step(net, obs, h)`,
   用公开 API 在 float64 下重写 `RNNPolicy.step` 的三步,并加一条断言把它锚回
   `forward_seq`,防止"两个错的东西互相印证"。
2. **同一个坑在打包后第二次踩。** `test_deploy_forward_agrees_with_a_real_pack`
   里还留着旧写法,导出完成后立刻 `ValueError: matmul ... size 128 is different
   from 1`。**结论:跳过型测试在它第一次真正运行之前,不能算测试。** 后来专门给
   它加了一句注释说明这个形状是从哪来的。

### 4.2 静默变弱:gru → gru 续训会丢掉记忆

`load_init_net` 原来无条件走 `RNNPolicy.from_mlp`,而 `from_mlp` 只搬 `_INHERITED`
那 16 个张量 —— 拿 `rl_VG_v2.0` 当起点时,它会把**训好的 GRU 与 P/bP 丢掉、重新随机
初始化**,而打印出来的话术还是"初始前向逐位等于 `rl_VG_v2.0`"。形状全对、一处不报错,
表现只是"续训了但没变强"。

- **修法**:检测到起点本身就是循环包时,按张量名整份 `load_state_dict`(30 个张量),
  顺带校验 `hidden` / `gru_hidden` 是否匹配(不匹配才报错);
- **绊线**:`tests/test_train.py::test_init_pack_gru_to_gru_copies_the_memory_too`,
  断言 `|bP|.max() > 0` 且 20 步前向与包逐位一致。用真实包跑,所以同时覆盖
  `load_pack_net` 解析 GRU 包那一段。

### 4.3 包能被读 ≠ 包能被注册

`RNNPolicy` 没有 `logits(obs)` 方法(只有 `step(obs, h)`),于是
`LazyPackNet.logits()` / `test_pack.py` 的注册表检查一遇到 GRU 包就 `AttributeError`。
**修法**:补一个 `logits(obs)` = "零隐状态下的单步前向",并在文档里写清它的语义
(它是"这一局刚开局、还没有任何历史"时网络会说的话,不是"这个观测单独看"的答案);
带记忆的调用方一律该用 `step(obs, h)`。

### 4.4 注册一个包要三处齐全

加了 `KNOWN_PACKS` 却忘了 `STATIC_OPPONENTS`,训练**在启动时**抛
`ValueError: 未知对手: rl_VG_v2.0`。这是 README §七判据 F 记过的那类事故,
`test_pack.py` 里有专门测试,但那次事故的教训不是"加个测试",而是
**"加一个包 = KNOWN_PACKS 一行 + STATIC_OPPONENTS 一条,缺一不可"**。

### 4.5 读数依赖名单:`evaluate()` 只有一条采样流

`evaluate()` 全程共用**一条** `NetPolicy` RNG 流,按对手在字典里的顺序依次消耗。所以
"某个对手的胜率"取决于它前面跑了多少局 —— 换名单、换顺序、换局数,读数就不可比。

本次实测到的后果很直观:同一个 v2.0 权重对 `rl_v5` 的读数

| 口径 | `rl_v5` |
|---|---|
| 10 对手名单、40 局、seed 20 | **0.650** ✗ |
| 10 对手名单、40 局、训练器种子 | 0.775 ✓ |
| 4 对手名单、200 局、seed 90000 | 0.765 ✓ |
| **单对手名单、400 局、seed 20** | **0.762** ✓ |

三个独立口径都落在 0.76~0.78,只有 seed-20 那条 10 对手流给出 0.650 —— 那是 40 局
(±0.079)在特定流位置上的噪声。**纪律:报任何读数必须带名单 + 局数 + 种子。**
`tests/diag_one_opp.py` 就是为此写的:单对手名单下那条流只服务一个人,读数最干净,
局数可以放心往上堆。

**还有一条更容易踩的**:同一名单、同一种子下,两个**不同的网络**也不是配对比较。
`play_match` 每局都用 `env.reset(seed=seed+g)` 重播环境,**棋盘和障碍对两边是同一份**;
但策略侧 `temperature=1.0` 是从那条共享流里采样的,而**一局消耗多少个随机数取决于
这一局有多长**,局长又取决于策略本身。于是网 A 跑到对手 k 时,流的位置已经和网 B
不一样了 —— 想拿"同种子 = 逐局配对"来缩小方差是**不成立**的,该堆局数还是得堆局数。
真正的配对要求每局重置策略与对手的 RNG,那是**评测语义的改动**,会让所有已有数字
失去可比性,所以没有做。

### 4.6 评论腐化(定时任务发现的)

本轮审注释发现并修掉了四处**与代码不符**的注释:

| 位置 | 原来的说法 | 实际 |
|---|---|---|
| `env/rules_vg.py` 模块 docstring | "连续失明 **2** 个阶段" | `MISS_THRESHOLD = 4` |
| `training/config.py` `seg_len` | "段长 32 ≈ **半个回合**的决策量" | 一局约 54 个决策点,32 ≈ 大半局 |
| `models/gru.py` 门序注释 | "拼在一个数组里" | 是 12 个独立命名的张量,没有哪个数组 |
| `rl_VG_v2.0/vg_rules.h` 第 1 行 | "`rl_VG_v1.0` 的规则层" | 本包的规则层(v2 换的是记忆,不是规则) |

这类注释比没有注释更坏:它们给的是**具体而错误的**信息,读者会照着它去理解代码。

### 4.7 环境

- **Windows GBK**:变异测试的 `subprocess.run` 抓 `UnicodeDecodeError: 'gbk' codec
  can't decode byte 0xb4`,加 `encoding="utf-8", errors="replace"` 解决。
- **日志缓冲**:训练输出重定向到文件时,评测段落可能攒到下一次 8KB 刷新才出现,
  "看起来卡住了"不代表卡住。解决办法是盯 **`metrics.csv` / `best.npz` 的 mtime**
  而不是盯日志 —— 那些是评测一结束就落盘的。

---

## 5. ⚠️ 负面发现:记忆的贡献落在噪声里

把包里 `P` / `bP` 强制归零(等价于"有记忆层但不用它",前向逐位退化成纯编码器),
在同一口径下重测:

| 对手 | 带记忆 | `P=bP=0` | 差 |
|---|---|---|---|
| `rl_v5` | 0.765 | 0.730 | +0.035 |
| `rl_VG_v0.2` | 0.935 | 0.910 | +0.025 |
| `rl_best040` | 0.905 | 0.915 | −0.010 |
| `rl_VG_v1.0` | 0.915 | 0.915 | +0.000 |
| 平均 | 0.8800 | 0.8675 | +0.0125 |

**记忆**在表示上是实实在在工作着的 —— 实测 `|P·h + bP| / |a3|` 的平均比值是
**0.29**,也就是记忆那一支的尺度是编码器输出的三成,不是可以忽略的零头。但它在
**胜率**上的贡献只有 +0.0125 的平均值,4 个对手里 3 个略好、1 个持平,单格 ±1SE 是
0.035 —— **全部落在噪声里**。

**所以必须诚实地说**:v2.0 相对 v1.0 的巨大提升(+0.385 vs v1.0)主要来自
**"在含 `rl_VG_v1.0` 的联盟里继续训练"**(联盟里它开局占 27.8% 的采样;
`league_weight_mode="room"` 按"谁还有余量"动态配重,到 update 40 时它是 9.9%,
而 `official_hunter` 拿走了 24.8%),而不是来自记忆架构本身。

为什么记忆没长起来?当时最可疑的解释是**优化压力不够**:`P` 从零出发,而它前面站着的
是一个已经很强的编码器,把 `P` 推大的边际收益很小;同时 lr(3e-4)是给一个全新网络调的,
对"只微调一小块新增参数"这件事偏大 —— 这也解释了早期那次全面回撤。

**这个猜测已经被 `runs/v2.1` 否证** —— 见 §6。结论是:不是"还没训够",而是这个架构 +
这套优化目标下,记忆支就长不到有正贡献的规模。(这后半句后来被 §7.4 收窄了:`P` 在
v2.3 上确实长到了 0.47,所以它只对 v2.1 那条谱系成立。)

**这条发现不改变交付结论**(目标读数四个全过线,且是两个独立种子各 200 局实测),但它
改变了**对成绩的归因**,而这正是探索报告该说清楚的事。

### 5.1 MLP 对照跑:把"记忆"和"多训一轮"分开

§5 的消融(`P=bP=0`)测的是"同一个网络里,记忆那一支关掉会怎样"。它测不了另一件事:
**这 0.385 的提升里,有多少只是"在含 `rl_VG_v1.0` 的联盟里又训了一轮"** —— 那件事
MLP 架构同样做得到。所以补了一个对照:同起点、同联盟、同种子、同步数,唯一差别是
`--arch mlp`(`runs/mlp_ctl`,`--init-pack rl_VG_v1.0`)。

200 局时对照读出的 `rl_v5` 是 **0.630**,比 v2.0 的 0.765 低 0.135 —— 看起来是"记忆
的贡献"。**但这个读数是噪声**,1000 局重测(单对手名单、seed 90000、±1SE 一并印出)
把它推翻了:

| 权重(同一个 `rl_v5`,1000 局,seed 90000) | 得分率 | ±1SE | 胜/平/负 |
|---|---|---|---|
| `rl_VG_v1.0`(起点,镜像对照) | 0.848 | 0.011 | 848/0/152 |
| `runs/v2.0/final.npz`(**GRU**) | **0.768** | 0.013 | 768/0/232 |
| `runs/v2.1/final.npz`(GRU,多 2.45 倍步数) | 0.734 | 0.014 | 734/0/266 |
| `runs/mlp_ctl/ckpt_u40.npz`(**MLP** 对照,同步数) | **0.700** | 0.014 | 700/0/300 |

**结论要说两遍才准确:**

1. **记忆确实有用,但只有在 1000 局这个分辨率上才看得见。** 同步数下 GRU 0.768 vs
   MLP 0.700,差 0.068 ± 0.020(两比例 z ≈ 3.5)。这是本报告里**唯一**一处能把"记忆"
   和"多训一轮"分开的对照,而它给的是正面的:同样多训一轮,带记忆的那一支在 `rl_v5`
   上多赢了 6.8 个百分点。
2. **200 局不足以读出这个差**(±1SE 就是 0.033,而效应量是 0.068)。0.630 那个数不是
   错的,是**测不准** —— 引用单格读数时先看 ±1SE,这是本报告 §4.5 那条纪律的另一面。

> 但也不要由此把记忆说成主角:0.068 只出现在 `rl_v5` 这一格上,而 §5 的消融显示
> 记忆支本身对胜率的贡献仍在噪声里。两件事不矛盾 —— 消融测的是"**已训好的**权重里
> 那一支的大小",对照测的是"**训练过程**里有它和没它的差别"。前者小,后者不小,
> 说明记忆的作用更像是**优化过程中的脚手架**,而不是最终前向里的主力。

---

## 6. v2.1:多训 2.4 倍步数,记忆反而缩了

### 6.1 这一轮想验证什么

§5 的猜测是"记忆没长起来 = 优化压力不够"。要是这个解释成立,那就该:**让起点更强**
(从 v2.0 整份继承,而不是从零开始)、**把学习率降下来**(3e-4 → 1.5e-4,别让新增的
小参数被大步长推飞)、**给它更长的生长时间**(400k 步,不早停)。三条一起上。

配置(`runs/v2.1/final.npz` 的 meta 实录):

```
--run-name v2.1 --arch gru --gru-hidden 128 \
--init-pack rl_VG_v2.0 --lr 1.5e-4 --steps 400000     # 不设 --stop-opponents
联盟(9):official_baseline/hunter/stalker/patrol, rl_v5, rl_VG_v0.2,
        rl_best040, rl_VG_v1.0, rl_VG_v2.0            # 把 v2.0 本体也放进陪练
```

启动日志确认起点正确:`架构 gru:整份继承 30 个张量(编码器 + 三个头 + GRU + P/bP)
—— 初始前向逐位等于 rl_VG_v2.0`。实际跑了 **98 个 update / 401408 步**(是 v2.0 的 2.45 倍)。

> 这句话本身还是一条证据:这套文案只有**修复后**的 gru→gru 分支才会打印
> (`selfplay.py:956`;坏路径走的是 `from_mlp`,打的是"已继承编码器与三个头(16 个张量)")。
> 所以 v2.1 **没有**踩 §4.2 那个静默丢记忆的坑 —— §7.3 第 1 条据此更正过。

### 6.2 结果:三条读数全都朝反方向走

| 读数 | v2.0(163840 步) | v2.1(401408 步) | 期望方向 | 实际 |
|---|---|---|---|---|
| 记忆支尺度 `\|P·h+bP\| / \|a3\|` | 0.2901 | **0.2695** | 变大 | ✗ 变小 |
| 消融(`P=bP=0`)的胜率贡献 | +0.0125 | **−0.0138** | 变大 | ✗ 转负 |
| 综合(4 对手 ×200 局 ×seed 90000) | 0.8800 | 0.8600 | 变大 | ✗ 变低 |

v2.1 在四个目标上仍然全过线(`rl_v5` 0.770、`v0.2` 0.890、`best040` 0.820、
`v1.0` 0.960),所以它**不是一次失败的训练** —— 它是"继续训下去并不能让记忆长起来"
这个结论的证据。多花 2.4 倍步数,记忆那一支的尺度**没变大反而缩小了**。

> 消融的两个数(+0.0125 / −0.0138)单格 ±1SE 都是 0.035,所以诚实的说法是
> **"记忆支没有正贡献",不是"它有负贡献"**。两个符号相反、量级相当的读数合起来
> 说明的就这一件事:它对胜率的影响小于测量分辨率。

### 6.3 结论:保留 `rl_VG_v2.0`

1. **不打包 v2.1。** 命名规则是"一次训练一个小版本",v2.1 这次训练已经跑完、权重留在
   `runs/v2.1/final.npz`,但它的战绩**没有超过** v2.0,再冻结一个包只会让"用哪个"变模糊。
2. **§5 的解释被替换。** 不是"训练不够久",而是在"编码器已经很强 + 记忆做成零初始化残差"
   这个组合下,`P` 的梯度信号始终不足以把它推到一个对胜率有用的规模。要验证"记忆到底
   能不能帮上忙",得换一个更能逼出记忆的**任务/对手设置**(例如对手带更强隐式状态、
   或者把部分观测做得更残缺),而不是继续加步数。
   **(注:v2.1 是三次训练里唯一一次 `P` 不从零出发的 —— 它继承 v2.0 的 `P`。这条结论
   后来被 §7.4 限定在 v2.1 这条谱系上,不再当成对该配置的一般性判断。)**
3. **`rl_VG_v2.0` 的交付结论不受影响** —— 它是两个独立种子上都四个目标全过线的那一份。

### 6.4 一个顺带观察(未深入)

v2.1 在**不在联盟里**的 `m3_v1` 上从 0.675 掉到 **0.250**(训练器自己的 10 对手评测)。
`m3_v1` / `m3_v2` 都不在 v2.1 的 9 个陪练里,是纯泛化探针。训得越久、越贴合这个联盟,
对联盟外对手的棋力退化越明显 —— 这是联盟训练的普遍现象,与记忆架构无关,记在这里
是因为它解释了"为什么综合没涨而单项有涨有跌"。

---

## 7. v2.3:关掉早停再训一轮,交付换成了这一份

### 7.1 与 v2.0 的差别只有三处

| 项 | `v2.0` | `v2.3` |
|---|---|---|
| 起点 | `--init-pack rl_VG_v1.0` | **同左**(GRU 与 `P/bP` 都是全新初始化) |
| 架构 / 超参 | GRU-128,`lr 3e-4`,`seg_len 32`,`ent_coef 0.01→0.003`,… | **完全一致** |
| 早停 | `stop_opponents=[4 个目标]`,`stop_winrate=0.7` | **`[]`(摘掉)** |
| 实际步数 | **163840**(update 40,**被早停拦下**) | **401408**(update 98,跑满)= **2.45×** |
| 联盟 | 8 个(4 个官方) | **10 个**(+`official_ambusher` / `official_weaver`) |

第三行值得单独说:**v2.0 不是在 400k 处停的**。它的预算是 400k,但第 40 次 update 时四个
目标已全部越过 0.7,早停判据把它拦下了 —— 也就是说 v2.0 的读数取自"预测预算的 41%"。
v2.3 摘掉那条判据,让同一套超参在同一张联盟上把预算跑完。

### 7.2 结果:8 格全涨

4 个目标 ×**400 局** × 两个独立种子,名单 `['rl_v5','rl_VG_v0.2','rl_best040','rl_VG_v1.0']`,
对手种子 12345,`rules=on`(`tests/diag_four_targets.py 400 <seed> <ckpt>`):

| 对手 | `v2.0` @90000 / @424242 | **`v2.3`** @90000 / @424242 | 差 |
|---|---|---|---|
| `rl_v5` | 0.750 / 0.735 | **0.785 / 0.835** | +0.035 / +0.100 |
| `rl_VG_v0.2` | 0.915 / 0.905 | **0.960 / 0.963** | +0.045 / +0.058 |
| `rl_best040` | 0.917 / 0.890 | **0.973 / 0.963** | +0.056 / +0.073 |
| `rl_VG_v1.0` | 0.882 / 0.887 | **0.970 / 0.945** | +0.088 / +0.058 |
| 平均 | 0.866 / 0.854 | **0.922 / 0.926** | +0.056 / +0.072 |
| **最低** | 0.750 / 0.735 | **0.785 / 0.835** | +0.035 / +0.100 |

**8 格全部上升**,没有一格持平或倒退。单格 ±1SE ≈ 0.019(n=400),除 `rl_v5` 的
seed-90000 那一格(+0.035,约 1.8SE)外,其余七格都超过 2SE。最紧的一格从 `rl_v5` 的
0.750 抬到 **0.785**,对 0.70 门槛的余量从 0.050 变成 0.085。

> 判读按 §1.1 的老规矩:`v2.0` 与 `v2.3` 在**同一份名单、同一个种子、同一次
> `evaluate()`** 里先后测出(`diag_v23.log` / `diag_v23_seed2.log` 里两行同屏),
> 所以这张表的差可比。**不要**拿它去和 §1.1 那张 n=200 的表做减法 —— `evaluate()`
> 只有一条采样流,局数一变读数就变(§4.5)。

### 7.3 ⚠️ 这**不能**算推翻 §6

§6 的结论是"再多训 2.4 倍步数没用",而 v2.3 恰好也训了 2.45 倍并且涨了。看着矛盾,
其实不是 —— **v2.3 一次改了三件事,什么都没隔离出来**:

1. **起点谱系不同。** v2.1 是 `--init-pack rl_VG_v2.0`,继承的是 v2.0 的**整份**权重 ——
   连已经训过的 GRU 和尺度 0.29 的 `P` 一起接着练;v2.3 是 `--init-pack rl_VG_v1.0`,
   只继承编码器与三个头,GRU 与 `P/bP` **全部从零开始**。

   > **更正**:这条原先写的是"v2.1 踩了 §4.2 那个 gru→gru 静默丢记忆的坑"。**那条是错的。**
   > §6.1 引的启动日志是"整份继承 30 个张量……",而这句话只有**修复后**的 gru→gru 分支
   > 才会打印(`selfplay.py:956`;坏路径打的是"已继承编码器与三个头(16 个张量)")。
   > `train_v2.1.log` 已经不在了,无法再核,但两条陈述不能同时成立 —— 以日志文案为准:
   > v2.1 走的是一条**已经修好**的续训路径,它记忆没长起来跟 §4.2 无关。
   > "起点谱系不同"这个混淆项本身照样成立,只是理由要换成上面这句。
2. **联盟更宽。** 8 → 10 个陪练(多两个官方 AI)。
3. **步数更多。** 163840 → 401408。

所以 v2.3 证明的是"**在 v2.0 的配置下把预算跑完能得到更强的策略**",**不是**"记忆这次
长起来了"。§5.1 那个"GRU vs 同步数 MLP"的对照在 v2.3 上**没有重跑**,所以它的结论
(同步数下 GRU 0.768 vs MLP 0.700)对 v2.3 仍然只是外推。

要干净地分开这三件事,得跑同一份 v2.0 起点、同一张窄联盟、**只**把 `stop_opponents`
摘掉。这一轮没跑。

### 7.4 补做:v2.3 的记忆消融

§5 与 §6.2 的消融分别在 v2.0 / v2.1 上做过,v2.3 这份是后补的。口径与那两轮**完全一致**
(`tests/diag_mem_ablation.py 200 90000 <ckpt>`:同一份 4 对手名单、200 局、seed 90000、
对手种子 102345、`rules=on`),所以三行数字可以并排看:

| 对手 | 带记忆 | `P=bP=0` | 差 |
|---|---|---|---|
| `rl_v5` | 0.805 | 0.760 | +0.045 |
| `rl_VG_v0.2` | 0.980 | 0.930 | +0.050 |
| `rl_best040` | 0.975 | 0.965 | +0.010 |
| `rl_VG_v1.0` | 0.955 | 0.960 | −0.005 |
| 平均 | 0.9287 | 0.9038 | **+0.0250** |

| 权重 | `\|P·h+bP\| / \|a3\|` | 消融差(平均) |
|---|---|---|
| `runs/v2.0/final.npz` @163840 | 0.2901 | +0.0125 |
| `runs/v2.1/final.npz` @401408 | 0.2695 | −0.0138 |
| **`runs/v2.3/final.npz` @401408** | **0.4730** | **+0.0250** |

**真正变了的是第二列,不是第三列。** `P` 零初始化,v2.0 与 v2.3 都是让它从零长;
v2.3 **一轮内部**就看得见单调增长 —— 同一份训练留下的三个检查点:

```
runs/v2.3/best.npz  @245760  -> 0.3650
runs/v2.3/last.npz  @327680  -> 0.4431
runs/v2.3/final.npz @401408  -> 0.4730
```

三个点一路上行(首尾**步数乘 1.63、尺度涨 30%**),而且同一次训练、同一张联盟、
同一套超参,没有别的变量。v2.1 是三次里**唯一**一次 `P` 不从零出发的(继承 v2.0 的
`P`),也是唯一一次尺度**缩了**的。

> 这直接收窄了 §6.3 那句"`P` 的梯度信号始终不足以把它推到一个对胜率有用的规模":
> 它描述的是 **v2.1 那条谱系**(续训一份已训的 `P`),不是"编码器强 + 零初始化残差"
> 这个配置本身 —— 同一个配置在 v2.3 上把 `P` 推到了 0.47。

**但 +0.0250 还不足以把结论翻过来**,三条:

1. **分辨率。** n=200 的单格二项 ±1SE 在 p≈0.95 时是 0.015、p≈0.78 时是 0.029
   (§5 / §6.2 引的 0.035 是 p=0.5 的最坏值);4 格平均在 0.01 量级。+0.0250 大致
   1.5~2.5SE —— 它是三个消融里第一个**符号朝正**的,不是第一个**显著**的。
2. **归零 `P` 不只是"撤掉信息"。** v2.3 的 `P` 支尺度是 0.47,归零等于把三个头的输入
   整体挪了一截。消融组变弱不能全算在"少了记忆"头上 —— 这条对 v2.0 / v2.1 同样成立,
   不是新问题,但它决定了这个差值不能当"记忆的贡献"读。
3. **它不隔离 §7.3 那三件事。** 消融回答的是"训好的 v2.3 里那一支有多大用",不回答
   "多训的 2.45 倍里有多少是记忆带来的" —— 那仍然只有 §5.1 的 MLP 对照能回答,
   而 v2.3 上**没有重跑**。

### 7.5 交付:`rl_VG_v2.3/`

```
python -m monet.cli export --ckpt runs/v2.3/final.npz --out rl_VG_v2.3/rl_weights.h
python tests/check_pack_headers.py rl_VG_v2.3
```

| 文件 | 说明 |
|---|---|
| `rl_VG_v2.3/rl_weights.h` + `rl_weights_part1..3.h` | 30 个张量,最大分片 7.00 MB(上限 8 MB) |
| `rl_VG_v2.3/rl_VG.cpp` | 与 `rl_VG_v2.0` 的**逐字节相同**(`c59646c5e55f0ce7f43d435f4a1647f6`) |
| `rl_VG_v2.3/obs_builder.h` | 与 v1.0 **md5 相同**(`bfdbd56c8eaef4aed1644084ac092cf4`) |
| `rl_VG_v2.3/vg_rules.h` | 与 v1.0 的规则**逐字相同**(只有开头注释不同) |

发出去之前验了三层:

1. **包 == 被评测的那一份。** 30 个张量对 `runs/v2.3/final.npz` 逐位相同,24 步连续前向
   logits/value 最大差 `0.000e+00`。
2. **部署侧翻译本 == Python 前向。** `tests/test_gru_deploy.py` 拿**包里真实的权重头**
   跑 16 步,最大差 `6.2e-15`。
3. **规则层没漂。** `tests/test_rules_vg.py` 的镜像测试改成**按包发现**后,`v1.0` /
   `v2.0` / `v2.3` 三个包的 `vg_rules.h` 常量一起对 `rules_vg.py` 核;另外把三个包的
   `obs_builder.h` 钉成与 v1.0 **逐位相同**。改之前这两条只认 `rl_VG_v1.0`,v2.3 的
   规则常量漂了是**不会报红**的(实测:把 v2.3 的 `kMissThreshold` 从 4 改成 3,
   改之前全绿,改之后退出码 1)。

**仍未验证的**:部署侧 C++ **没有被编译过**(本机无工具链)。与 §3 同一个限制。

---

## 8. 交付物清单

| 文件 | 说明 |
|---|---|
| `rl_VG_v2.0/rl_weights.h` | 权重头,**纯 include**(30 个张量拆成 3 个分片) |
| `rl_VG_v2.0/rl_weights_part1..3.h` | 分片,最大 7.02 MB(上限 8 MB) |
| `rl_VG_v2.0/rl_VG.cpp` | 部署壳:`forward` + `gru_step` + 全局 `g_h` + 安全外壳 |
| `rl_VG_v2.0/obs_builder.h` | 与 v1.0 **md5 相同**(`bfdbd56c8eaef4aed1644084ac092cf4`) |
| `rl_VG_v2.0/vg_rules.h` | 与 v1.0 的规则**逐字相同**(只有第 1 行注释不同) |

> **当前交付物是 §7 的 `rl_VG_v2.3/`。** 下表是首次达成时的 `rl_VG_v2.0/`;两者的
> `rl_VG.cpp`、`obs_builder.h`、`vg_rules.h` **逐字节相同**,只有权重头不同。

自检:

```
python tests/check_pack_headers.py rl_VG_v2.3    # 架构 GRU / include 链 / 符号 / 维度自洽
python -m pytest tests/ -q                      # 197 passed
```

**包里的权重与 `runs/v2.0/final.npz` 逐位相同**(40 步连续前向,最大差 `0.000e+00`)
—— 也就是"发出去的这一份 == 被评测的那一份"。`rl_VG_v2.3` 同样验过,**见 §7.4**。

**未打包但保留的**:

| 文件 | 说明 |
|---|---|
| `runs/v2.1/final.npz` | §6 那一轮的权重(401408 步)。**没有冻结成包** —— 它没有超过 v2.0 |
| `runs/v2.0/metrics.csv` / `train_metrics.csv` | 逐次评测 / 逐 update 的训练记录 |
| `runs/v2.1/train_metrics.csv` | 同上,98 行 |
| `runs/v2.3/best.npz` | §7 那一轮的**中途最好**权重(step 245760)。**没有打包** —— 逐种子比:它 @90000 最低格 0.833 胜过 `final` 的 0.785,但 @424242 反过来(0.785 vs 0.835);按两个种子平均,它最低格 0.809 与 `final` 的 0.810 打平,而**均值低 0.031**(0.894 vs 0.924)。最低格是判据,但打平时取均值更高的那份 |
| `runs/v2.3/metrics.csv` / `train_metrics.csv` / `curves.png` | 逐次评测 / 逐 update / 曲线 |

### 复现这份报告里的每个数

```
# §1.1 的核心读数(两个种子)
python tests/diag_v2_control.py 200 90000  runs/v2.0/final.npz
python tests/diag_v2_control.py 200 424242 runs/v2.0/final.npz
python tests/diag_v2_control.py 200 90000  runs/v2.1/final.npz

# §5 / §6.2 / §7.4 的记忆消融(+ 记忆支尺度)
python tests/diag_mem_ablation.py 200 90000 runs/v2.0/final.npz
python tests/diag_mem_ablation.py 200 90000 runs/v2.1/final.npz
python tests/diag_mem_ablation.py 200 90000 runs/v2.3/final.npz

# §5.1 的 MLP 对照(1000 局,四个权重一起量,±1SE 一起印)
python tests/diag_one_opp.py rl_v5 1000 90000 runs/v2.0/final.npz \
    runs/v2.1/final.npz runs/mlp_ctl/ckpt_u40.npz

# 单个对手的高精度读数(单对手名单,±1SE 会一起印出来)
python tests/diag_one_opp.py rl_v5 400 20

# §7.2 的 v2.3 读数(v2.0 与 v2.3 同屏,便于直接比)
python tests/diag_four_targets.py 400 90000  runs/v2.3/final.npz runs/v2.0/final.npz
python tests/diag_four_targets.py 400 424242 runs/v2.3/final.npz runs/v2.0/final.npz
python tests/diag_four_targets.py 400 90000  runs/v2.3/best.npz
python tests/diag_four_targets.py 400 424242 runs/v2.3/best.npz
```

**报数纪律**:`evaluate()` 在所有对手之间共用**同一条采样流**,所以一个对手的读数会随
名单的长度和顺序变(§4.5)。上面每条命令的名单、局数、种子都写死在脚本里 —— 引用这些
数字时必须连着它们一起引,不要跨名单做减法。

**残留风险**:部署侧 C++ **没有被编译过**(本机无工具链)。见 §3。
